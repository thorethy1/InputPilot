## Summary
<!-- What changed and why (1–3 bullets). -->

## Test plan
- [ ] `pio test -e native` (or rely on CI)
- [ ] `pio run -e esp32s3` (or rely on CI)
- [ ] Docs updated if flash/API/behavior changed
- [ ] No secrets (`wifi_secrets.h`, `config.env`, passwords) in the diff

## Hardware (if on-device verified)
<!-- Board model, USB VID seen, optional: macOS E2E / WiFi / BLE. -->
