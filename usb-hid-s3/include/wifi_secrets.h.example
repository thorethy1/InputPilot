// Copy to wifi_secrets.h (gitignored) and fill in your 2.4 GHz network.
// Never commit wifi_secrets.h.
#pragma once

#define WIFI_SSID "YourNetwork"
#define WIFI_PASS "YourPassword"

// Optional Soft-AP password (8+ chars for WPA). Empty / omit = open AP.
// #define WIFI_AP_PASS "setup-secret"

// Optional LAN control token for HTTP (X-API-Token / Bearer), TCP, and BLE.
// When set, Soft-AP WiFi provisioning stays open; STA control requires the token.
// #define CONTROL_API_TOKEN "change-me"
